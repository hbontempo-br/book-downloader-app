import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

export const homepageStyles = makeStyles((theme: Theme) => createStyles({
  root: {
  },
  table: {
    marginLeft: 'auto',
    marginRight: 'auto',
    paddingBottom: '40px',
    paddingTop: '40px',
    textAlign: 'center',
    width: '80vw',
  },
}));
